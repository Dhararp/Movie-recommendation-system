# Movie-recommendation-system
The Movie Recommendation System is a web application built using Flask and Python that suggests similar movies based on a user’s selection. It stores search history in MySQL and displays user search trends with a regression graph using Matplotlib. This project combines recommendation, data analysis, and visualization.
